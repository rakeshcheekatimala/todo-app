input {
  beats {
    port => 5044
  }
  tcp {
    port => 5000
  }
}

filter {
  if [type] == "todo-backend" {
    json {
      source => "message"
    }
  }
}

output {
  elasticsearch {
    hosts => ["elasticsearch:9200"]
    index => "todo-logs-%{+YYYY.MM.dd}"
  }
} 